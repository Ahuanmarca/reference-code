enum GameState {
  IDDLE = 'IDDLE',
  LOADING = 'LOADING',
  PLAYING = 'PLAYING',
  PAUSED = 'PAUSED',
}

console.log(GameState.IDDLE);

enum ArrowKeys {
  UP = 'UP',
  DOWN = 'DOWN',
  LEFT = 'LEFT',
  RIGHT = 'RIGHT',
}

console.log(ArrowKeys);
console.log(ArrowKeys.UP);
